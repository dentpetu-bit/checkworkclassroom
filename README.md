# QR Score Studio by Kru Dew — v7 Zero Score Red

แก้ไขจาก v6:
- ช่องคะแนนที่เป็น `0` ในหน้ารายงานเป็นสีแดงทันที
- ช่องคะแนนเต็มเป็นสีเขียวอ่อน
- ตัวเลขลำดับชิ้นงานไม่หาย แม้ `sort_order` มีปัญหา จะ fallback เป็นลำดับในตาราง
- แก้คะแนนในรายงานแล้วสีเปลี่ยนอัตโนมัติ

วิธีอัปเดต:
1. อัปโหลดไฟล์ทั้งหมดทับของเดิมใน GitHub
2. เปิดเว็บแล้วกด `Ctrl + Shift + R`
3. หรือเปิด `https://dentpetu-bit.github.io/checkworkclassroom/?v=7`

หมายเหตุ: ไม่จำเป็นต้องรัน `schema.sql` ใหม่ หากฐานข้อมูล v6 ใช้ได้แล้ว
